package hibernate.entity;


public enum currency {
        RON("RON"),
        EURO("EURO");

        String currency;

        currency(String currency) {
            this.currency = currency;
        }
    }





















